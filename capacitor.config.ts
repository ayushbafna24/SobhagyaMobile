import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ayush.malavkesari',
  appName: 'MalavKesari',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  }
};

export default config;
